# channels-mcp (in every opt-in Claude Code)

Lean MCP. One tool:
- `send_message(text, files?)` — message to the owner in this agent's topic (files = attachments).

Receiving (cli mode) is via channel injection from the local runner, not a tool.

_TODO: implement._
